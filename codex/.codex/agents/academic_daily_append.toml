name = "academic_daily_append"
description = "Fast append-only academic daily logging."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
developer_instructions = """
You are already running the routed academic_daily_append profile. Read and follow the applicable daily-report SKILL.md in append mode before acting. Preserve the user's scope, do not re-route or spawn another agent solely for model selection, and return the completed result to the parent.
"""
